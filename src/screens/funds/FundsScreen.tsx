"use client";

import { PlaceholderScreen } from "@/screens/common/PlaceholderScreen";

/**
 * 资金：北向/主力/两融等入口占位（后续可接 fflow、板块资金等）。
 */
export function FundsScreen() {
  return <PlaceholderScreen screenKey="funds" dataCmp="FundsScreen" />;
}
