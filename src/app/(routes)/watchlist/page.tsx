import { WatchlistScreen } from "@/screens/watchlist/WatchlistScreen";

/**
 * 自选页路由：`/watchlist`；行情主图为 `/` 或 `/?code=六位代码`。
 */
export default function WatchlistPage() {
  return <WatchlistScreen />;
}
