"use client";

import { PlaceholderScreen } from "@/screens/common/PlaceholderScreen";

/**
 * 设置：主题、刷新间隔、数据源说明等入口占位。
 */
export function SettingsScreen() {
  return <PlaceholderScreen screenKey="settings" dataCmp="SettingsScreen" />;
}
