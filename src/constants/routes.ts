/**
 * 前端路由路径常量（页面在 `src/app/(routes)`，`api` 在 `src/app/api`）。
 */
export const ROUTES = {
  home: "/",
  watchlist: "/watchlist",
  funds: "/funds",
  research: "/research",
  messages: "/messages",
  settings: "/settings",
} as const;
